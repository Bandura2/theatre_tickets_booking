# --- Основний фреймворк ---
Django>=5.0,<6.0            # Головний веб-фреймворк (забезпечує адмінку, ORM, маршрутизацію)

# --- REST API ---
djangorestframework>=3.14   # Інструмент для побудови REST API (серіалізатори, вьюшки)
django-filter>=23.0         # Дозволяє легко фільтрувати дані (наприклад, фільми за жанром)

# --- Зв'язок з фронтендом ---
django-cors-headers>=4.3    # Критично важливо! Дозволяє JavaScript-фронтенду робити запити до бекенду

# --- Робота з медіа ---
Pillow>=10.0                # Бібліотека для обробки зображень (постери фільмів, схеми залів)

# --- Документація (Важливо для курсової) ---
drf-yasg>=1.21              # Автоматично генерує Swagger-документацію (дуже круто виглядає у звіті)

# --- Змінні оточення ---
python-dotenv>=1.0          # Щоб ховати секретні ключі та налаштування БД

# --- База даних (Опціонально) ---
# psycopg2-binary           # Розкоментуй, якщо будеш використовувати PostgreSQL замість SQLite